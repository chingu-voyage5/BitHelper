function projectReducer (state = {}, action) {
    switch (action.type) {
        case 'ADD_PROJECT':
            return state;
        default:
            return state;
    }
}

export default projectReducer;